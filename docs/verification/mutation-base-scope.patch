diff --git a/pyproject.toml b/pyproject.toml
index 8151e38..6876fbd 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -146,7 +146,7 @@ only_mutate = [
     "src/bevcalib/analysis/estimands.py",
     "src/bevcalib/analysis/aggregate.py",
 ]
-also_copy = ["configs", "schemas", "docs"]
+also_copy = ["configs", "schemas", "docs", ".agents", ".github"]
 # One test file is excluded because it READS first-party source rather than
 # running it: the envelope conformance check scans `src/` for any mention of
 # P1's package, and mutmut rewrites `src/` to create mutants, so inside the
diff --git a/tests/contract/test_mutation_scope.py b/tests/contract/test_mutation_scope.py
index 3330281..68d67ab 100644
--- a/tests/contract/test_mutation_scope.py
+++ b/tests/contract/test_mutation_scope.py
@@ -18,3 +18,21 @@ def test_mutation_scope_retains_the_pure_core_and_formal_pairing() -> None:
     ]
     assert all((root / path).is_file() for path in required)
     assert all(any(fnmatch.fnmatchcase(path, pattern) for pattern in patterns) for path in required)
+
+
+def test_mutation_sandbox_copies_non_source_inputs_consumed_by_contract_tests() -> None:
+    root = Path(__file__).parents[2]
+    config = tomllib.loads((root / "pyproject.toml").read_text(encoding="utf-8"))
+    copied_roots = {Path(path) for path in config["tool"]["mutmut"]["also_copy"]}
+    consumed = (
+        Path(".agents/skills/verifying-nuscenes-transforms/SKILL.md"),
+        Path(
+            ".agents/skills/verifying-nuscenes-transforms/scripts/validate_transform_chain.py"
+        ),
+        Path(".github/workflows/ci.yml"),
+        Path(".github/workflows/pages.yml"),
+        Path(".github/workflows/release.yml"),
+    )
+
+    assert all((root / path).is_file() for path in consumed)
+    assert all(any(path.is_relative_to(copied) for copied in copied_roots) for path in consumed)
